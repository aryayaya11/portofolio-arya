import * as en from "./portfolio-en";
import * as id from "./portfolio-id";

// Keep types from English version for convenience
export type { TechIcon, DeviceScreenshots, Project } from "./portfolio-en";

export const portfolioData = {
  en,
  id
};
